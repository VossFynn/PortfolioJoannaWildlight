# PROGRESS — Joanna Wildlight Portfolio

## Stand
- Zuletzt erledigt (Session 12, Production-Build-Fix Impressum/Datenschutz): Der Netlify-Production-Build für Commit `8f5759b` (Session 9–11 zusammengefasst) schlug fehl (`Failed during stage 'building site': Build script returned non-zero exit code: 2`). Ursache per Netlify-Dashboard-Deploy-Log verifiziert (nicht geraten): `payload migrate` lief fehlerfrei durch, aber `next build` crashte beim Prerendering von `/datenschutz` mit `TypeError: Cannot read properties of undefined (reading 'split')` — der `Accent`-Helper ruft `.split()` auf einem CMS-Textfeld auf, das in Produktion `undefined` war, weil die Payload-Globals `impressum-page`/`datenschutz-page` dort **noch nie befüllt wurden** (`npm run seed:legal` wurde bisher nur gegen die lokale Dev-DB ausgeführt, nie gegen die Produktions-DB — per `curl` gegen die Live-Seite verifiziert: `/impressum` zeigte nur ein nacktes "Impressum"-H1 ohne jeglichen Rechtstext). Fix in `lib/content/provider.ts` (`getLegalPage`): alle Felder (`meta`, `kicker`, `headline`, `subtitle`, `badgeLabel`, `sections[]`, `note`) bekommen jetzt `?? ""`/`?? []`-Fallbacks, bevor sie an die Komponenten weitergereicht werden — verhindert den Crash unabhängig vom DB-Zustand. Verifiziert: `npx tsc --noEmit` und `npx eslint …` sauber, vollständiger `npx next build` lokal erfolgreich (alle 13 Routen inkl. `/impressum` und `/datenschutz` prerendered, keine Fehler außer dem bekannten harmlosen macOS-`objc`-Warning zu doppelten sharp-libvips-Versionen aus den Known-Environment-Quirks). Migration selbst (`20260720_222202_legal_page_sections`) war **nicht** die Fehlerquelle — trotz `ADD COLUMN ... NOT NULL` ohne `DEFAULT` auf potenziell nicht-leeren Tabellen lief sie in Produktion sauber durch (Global-Dokument existierte dort offenbar noch nicht als echte Zeile). Kommittet und nach `main` gepusht, damit Netlify neu deployt. Neuer Production-Deploy (`51476dc`) per Netlify-Dashboard-Log verifiziert grün (`ready`), `/impressum` und `/datenschutz` liefern wieder 200 statt Build-Crash.

Rechtstext ins Produktions-Payload eingetragen: User hat `/admin`-Zugangsdaten geteilt; statt manuellem Abtippen im Admin-Panel (fehleranfällig bei den vielen Sections/Paragraphs/Pills) per Payload-REST-API erledigt — `POST /api/users/login` für ein JWT, dann `POST /api/globals/impressum-page` bzw. `/datenschutz-page` (bei Payload-Globals ist es **POST für Updates, nicht PATCH** — PATCH liefert fälschlich "Route not found" statt eines Access-/Method-Fehlers, per Context7-Payload-Doku verifiziert statt geraten). Payload-Body nicht von Hand aus `content/legal.ts` abgetippt, sondern per `payload run` einmalig real aus dem TS-Modul exportiert und programmatisch in die Seed-Struktur (`{text}`-Wrapper für Arrays) transformiert — schließt Übertragungsfehler aus. Nach ~60s ISR-Revalidate per `curl` verifiziert: beide Live-Seiten zeigen jetzt den vollen Rechtstext (Headlines, Sections, Notiz). Temporäre Dateien mit dem Admin-JWT wurden danach aus dem Scratchpad gelöscht. `DATABASE_URI` in Netlify ist als "sensitive" markiert und blieb während der gesamten Session unzugänglich (auch beim Schreiben in eine Datei bereits redigiert) — kein Zugriffsversuch darüber hinaus unternommen.
- Zuletzt erledigt (Session 11, Trust-Zeile — echter Google-Review-Link): User wollte ursprünglich einen live von Google abgerufenen Sterne-Durchschnitt + Direktlink zum Bewerten. Nach Rückfrage entschieden: **live Rating-Abruf abgebrochen** (hätte einen Google-Cloud-Places-API-Key + Billing-Setup gebraucht, User wollte das nicht) — der Sterne-Text bleibt wie bisher statisch über das CMS-Feld `home-page.trust.googleStarsLine` pflegbar (aktuell "5,0 bei Google", stimmt mit dem echten Live-Wert überein, im Browser gegen ihr echtes Google-Profil verifiziert: 5,0 bei 12 Rezensionen). Der zweite Teil des Wunsches — Klick auf den Button soll direkt zur Bewertungsseite führen — wurde umgesetzt: `trust.reviewUrl` zeigte bisher auf `https://g.page/joannawildlight`, einen **toten Link** (Vanity-Kurzname nie beansprucht/abgelaufen — leitet nur auf eine generische Google-Suche nach dem Literal-String "joannawildlight" um, nicht auf ihr Profil; per `curl -L` verifiziert). Echten Google-Business-Eintrag über den vom User geteilten `share.google`-Link aufgelöst (führt zu `ludocid=3003291581877631115`, ihre Google-CID) und daraus die stabile Feature-ID (`0x47b0d77c9994f3f3:0x29add5c816bcb88b`) aus der Maps-URL extrahiert. Es gibt **keinen keylosen Weg zum echten Places-`place_id`** (ChIJ…) für den offiziellen 1-Klick-`search.google.com/local/writereview?placeid=`-Link — das erfordert zwingend die Places API. Stattdessen `reviewUrl` auf einen direkt navigierbaren Maps-Deep-Link mit `!9m1!1b1`-Flag gesetzt, der **ohne Login/Session-Abhängigkeit** (reine ftid-basierte URL, kein Cookie-Zustand) direkt auf dem Rezensionen-Tab landet, mit dem "Rezension schreiben"-Button prominent oben — ein Klick von dort zum eigentlichen Compose-Formular, praktisch gleichwertig zum offiziellen Link. Geändert: `content/home.ts` (Seed-Quelle) + direkt per `psql` in der lokalen Dev-Postgres-DB (`home_page.trust_review_url`), da Content aus Postgres kommt, nicht aus `content/*.ts` (siehe Content-Seam-Doku oben). End-to-End im Chrome-Browser gegen `npm run dev` verifiziert: Klick auf "Erfahrungen teilen" auf der Startseite navigiert korrekt zu Joannas echtem Maps-Eintrag, Rezensionen-Tab, 5,0★/12 Rezensionen sichtbar. **Offen/nicht von hier erledigbar**: die **Produktions-DB (Neon)** hat vermutlich noch den alten toten Link (nur lokale Dev-DB wurde gepatcht) — der User muss den neuen `reviewUrl`-Wert noch selbst in der Live-`/admin`-Oberfläche unter Homepage → Trust-Zeile → Review-URL eintragen (Wert steht in `content/home.ts:79`); kein Code-Deploy nötig, ISR zieht die Änderung automatisch innerhalb ~60s nach.
- Zuletzt erledigt (Session 10, Kontaktformular-Benachrichtigungsmail): Admin kann jetzt in `/admin` → Site Settings → neue Gruppe "Notifications" eine `contactEmail` hinterlegen, die bei jeder neuen Kontaktanfrage benachrichtigt wird. Umsetzung nach Payload-Doku (Context7) verifiziert statt geraten: `@payloadcms/email-resend` (`resendAdapter`) in `payload.config.ts`, nur aktiv wenn `RESEND_API_KEY` gesetzt ist (sonst `email: undefined`, alles läuft wie zuvor ohne Mailversand — Fallback-Pattern war schon als Kommentar in `lib/contact/submit.ts` angelegt). Neuer `afterChange`-Hook (`operation === 'create'`) in `collections/ContactSubmissions.ts`, ausgelagert nach `lib/contact/notify.ts`: liest `contactEmail` per Local API aus dem SiteSettings-Global (Access-Control wird dabei serverseitig ignoriert, Local API läuft standardmäßig mit `overrideAccess`), formatiert Name/E-Mail/Telefon/Anlass/Quelle/Nachricht als einfaches HTML, `replyTo: doc.email` gesetzt (Payload reicht das 1:1 an Resends `reply_to` durch — Admin kann im eigenen Postfach direkt antworten, landet beim Absender). Skip still (kein Fehler, keine Aktion) wenn kein Adapter konfiguriert oder `contactEmail` leer. Feld selbst hat `access.read` auf eingeloggte User beschränkt, obwohl `site-settings` als Global `read: () => true` hat (Footer/Nav/Logo müssen öffentlich lesbar bleiben) — sonst wäre die Benachrichtigungsadresse über `/api/globals/site-settings` öffentlich abrufbar gewesen. Migration `20260720_223835_add_contact_notification_email` per `migrate:create` erzeugt.
  - **Nebenbei gefundener + behobener Bug im bestehenden Migrations-Setup**: `npm run migrate` hing lokal komplett (musste per SIGTERM abgebrochen werden). Ursache: `premigrate`-Skript (`scripts/clear-dev-migration-marker.mjs`, seit Session 9 im Einsatz) lief bisher über nacktes `node scripts/…mjs` — das lädt `.env.local` nicht (nur Payloads eigene CLI-Binary macht das intern über `dist/bin/loadEnv.js`, ein bloßer `node`-Aufruf nicht). Lokal liegt `DATABASE_URI` ausschließlich in `.env.local` (auf Netlify dagegen als echte Prozess-Env gesetzt, deshalb dort nie aufgefallen) → das Skript sah `DATABASE_URI` als leer an und übersprang sich selbst still, der alte `batch=-1`-Dev-Marker blieb in `payload_migrations` stehen, und `payload migrate` öffnete daraufhin seinen interaktiven Bestätigungs-Prompt ohne TTY — hängt für immer. Fix: `premigrate` läuft jetzt über `payload run scripts/clear-dev-migration-marker.mjs` (gleiches Muster wie `seed`/`seed:legal`), damit die Payload-Bin-Umgebungsladung greift. **Zweiter, dadurch aufgedeckter Folgefehler**: die Marker-Lösch-Heuristik selbst markierte beim ersten (jetzt erfolgreichen) Lauf die brandneue Migration fälschlich als "bereits angewendet" (`schemaAlreadyPushed`-Check prüft nur, ob die `users`-Tabelle existiert, nicht ob *diese konkrete* Migration wirklich im Schema steckt — die Spalte fehlte nachweislich, per `psql \d site_settings` verifiziert). Manuell den Fehleintrag aus `payload_migrations` gelöscht und `migrate` erneut laufen lassen, danach Spalte real vorhanden. Dieselbe Heuristik-Schwäche kann bei jeder künftigen neuen Migration erneut zuschlagen, solange irgendwann nochmal ein Dev-Marker auftaucht — nicht weiter gehärtet (Scope dieser Session), aber notiert für den Fall, dass `npm run migrate` mal wieder eine Migration "anwendet", ohne dass sich das Schema ändert: erst per `psql` verifizieren, ob die Spalte/Tabelle wirklich da ist, bevor man dem grünen Log traut.
  - Verifiziert: `tsc --noEmit` sauber, `payload generate:types` neu gelaufen (`SiteSetting.notifications.contactEmail` korrekt in `payload-types.ts`), End-to-End im Chrome-Browser gegen lokale DB (frischer Admin-User angelegt, da lokale Postgres-Instanz noch nie eingeloggt war): Notifications-Feld in Site Settings gespeichert, öffentliches Kontaktformular unter `/kontakt` mit Testdaten abgeschickt (Nutzer-Freigabe vorher eingeholt) → `POST /api/contact-submissions 201`, Eintrag korrekt in Postgres, kein Fehler-Log aus dem `afterChange`-Hook (Resend-Adapter war ohne "no email adapter"-Warnung beim Dev-Server-Start aktiv) — spricht stark dafür, dass die Mail tatsächlich verschickt wurde; Empfang selbst nicht durch Claude geprüft (kein Gmail-Zugriff angefordert). **Bekannte Einschränkung**: mit der Standard-Absenderadresse `onboarding@resend.dev` (keine verifizierte eigene Domain) erlaubt Resend nur Versand an die E-Mail-Adresse des eigenen Resend-Accounts — für Versand an beliebige `contactEmail`-Adressen muss Joanna eine eigene Domain in Resend verifizieren.
- Zuletzt erledigt (Session 9, Impressum/Datenschutz — Design-Umsetzung): Claude Design (`claude.ai/design`, Projekt "Joanna Wildlight", `Final.dc.html` → importiert `Rechtliches.dc.html`) per `DesignSync`-Tool gelesen und 1:1 umgesetzt (Hero mit Monospace-Kicker/Headline/Sticker/Sonnenkreis, Laufband, nummerierte rotierte Karten, hervorgehobene Schluss-Notiz). Die Rechtstexte aus `RECHTSTEXTE.md` (Session 8) wurden dabei in die Karten-Struktur des Designs einsortiert, redaktionelle `[TODO: …]`-Hinweise (Steuernummer, DB-Region, R2-Jurisdiktion, Resend-Absatz) bewusst NICHT auf die Live-Seite übernommen (entweder noch nicht bestätigt oder laut DEPLOYMENT.md noch nicht produktiv). Kicker/Abschnittstitel nutzen `§ 5 DDG` statt `§ 5 TMG` — das Design selbst hatte das bereits korrekt aktualisiert (TMG-Impressumspflicht ist ins Digitale-Dienste-Gesetz übergegangen). Die bisherigen Seiten waren nur "Headline + freies Rich-Text-Feld"; da inhaltlich viel mehr Struktur nötig war (nummerierte Abschnitte, optionale Aufzählung, optionale Rechte-Chips) und der Rest des Codebase durchgängig auf einfache String-Arrays statt Lexical-RichText setzt (richText war bisher nirgends sonst im Einsatz), wurden `ImpressumPage`/`DatenschutzPage`-Globals umgebaut: `kicker`, `subtitle`, `badgeLabel`, `marqueeItems[]`, `sections[]` (je `number`, `title`, `paragraphs[]` mit `\n` als Zeilenumbruch-Konvention wie bei `AccentedText`, optional `list[]`/`pills[]`) und `note{title,body}` — neue Helper `legalSectionsField`/`legalNoteField` in `globals/fields/shared.ts`. Rendering in neuer, von beiden Seiten geteilter Komponente `components/sections/LegalPageSections.tsx`: Karten laufen über CSS `columns-2` (nicht `grid`) mit `break-inside-avoid`, damit unterschiedlich lange Karten sich wie im Design selbst balancieren statt zeilenweise auszurichten; Fließtext bekommt einen kleinen Auto-Linkify-Helfer (bare E-Mail/URL → `<a>`), die Rechte-Chips im Datenschutz-Abschnitt 03 heben den letzten Eintrag ("Beschwerde bei der Aufsichtsbehörde") farblich hervor, exakt wie im Design. Content in neuer `content/legal.ts`, geseedet über ein eigenes `scripts/seed-legal.ts` (`npm run seed:legal`) statt über den Haupt-`seed.ts`-Lauf, weil dieser bei nicht-leerer Media-Collection früh abbricht (Duplikatschutz) — `updateGlobal` ist aber idempotent und darf beliebig oft laufen; `scripts/seed.ts` wurde trotzdem ebenfalls um die beiden Globals ergänzt (für einen zukünftigen kompletten Fresh-Seed). Migration `20260720_222202_legal_page_sections` per `payload migrate:create` erzeugt — das Tool fragt bei jedem neuen Feld interaktiv "created or renamed?" (drizzle-kit/`hanji`-Prompt, braucht echtes TTY/Raw-Keypresses; einfaches Pipe von Enter über `yes ""` hängt sich in einer Busy-Loop auf, weil `setRawMode` auf einem Nicht-TTY-Stream keine Keypress-Events liefert) — gelöst mit `expect` (auf macOS vorinstalliert), das bei jedem Prompt einfach Enter sendet und damit die bereits vorausgewählte "create column"-Option bestätigt. Zusätzliche Falle beim Anwenden der Migration: `npm run migrate` lief zunächst durch das `premigrate`-Hook-Skript (`clear-dev-migration-marker.mjs`), das fälschlich einen "Dev-Push bereits erfolgt"-Zustand erkannte (Heuristik prüft nur, ob die `users`-Tabelle existiert, nicht ob *diese* Migration schon wirklich angewendet wurde) und die neue Migration nur als "bereits angewendet" in `payload_migrations` einträgt, ohne die SQL auszuführen — per `psql` verifiziert (Spalten fehlten), den Fehleintrag gelöscht und `migrate` erneut laufen lassen, danach Spalten/Tabellen korrekt vorhanden. Nebenbei bemerkt und behoben: `content/site.ts` hatte den `Cookies`-Footer-Link schon in Session 8 entfernt, aber nie neu geseedet — die Live-DB zeigte den Link weiterhin; per einmaligem Wegwerf-Skript (`payload run`, danach gelöscht) `site-settings.footer` synchronisiert. Verifiziert: `tsc --noEmit` und `eslint` sauber, beide Seiten im Chrome-Browser bei 1440px und 390px durchgescrollt (Hero/Sticker/Sonnenkreis-Platzierung, Laufband, Karten-Rotation/-Nummerierung, Rechte-Chips inkl. Hervorhebung, mailto-Link, Notiz-Karte, Footer ohne Cookies-Link) — sieht am Design-Vorbild identisch aus, keine Konsolenfehler.
- Zuletzt erledigt (Session 8, Rechtstexte-Entwurf Impressum/Datenschutz/Cookies): Joanna hatte auf der alten Wix-Seite (`joannawildlight.de/impressum`, `/datenschutz`, `/cookies`) bereits Rechtstext eingepflegt (Wix-eRecht24-Generator), der aber auf die alte Infrastruktur (Wix-Hosting, Wix-CDN) zugeschnitten war und teils widersprüchlich war (Adresse "Varreler Bäke" im Impressum vs. "Vareler Bäke" im Datenschutztext — Tippfehler geklärt, korrekt ist "Varreler"). Alle drei alten Seiten per Chrome-Browser-Automatisierung ausgelesen (der Datenschutztext lag als separates Wix-gehostetes HTML in einem iframe, `get_page_text`/WebFetch allein lieferten nur die Wix-SPA-Hülle ohne Inhalt — musste die iframe-`src` per `javascript_tool` ermitteln und direkt aufrufen). Vor dem Formulieren erst recherchiert, was die *neue* Seite technisch tatsächlich tut (Subagent-Grep über den ganzen Code): kein Analytics/Tracking irgendeiner Art installiert, keine Cookies für normale Besucher:innen (nur Payload-Admin-Login-Session-Cookie unter `/admin`), Instagram nur als einfacher `<a>`-Link (kein eingebettetes Widget, kein Datenabfluss an Meta beim bloßen Seitenaufruf), `next/font/google` lädt Schriften self-hosted beim Build (keine Laufzeit-Anfrage an Google). Kontaktformular-Felder aus `components/sections/ContactForm.tsx`/`collections/ContactSubmissions.ts` übernommen (Name, E-Mail, Telefon optional, Anlass, Nachricht, Quelle optional; öffentlicher `create`, `read` nur eingeloggt). Netlify/Cloudflare/Resend-Firmierungen und DPA/SCC-Hinweise per WebFetch aus deren aktuellen Datenschutzerklärungen verifiziert statt aus dem Gedächtnis geraten (Netlify, Inc., 101 2nd Street, San Francisco; Cloudflare, Inc., 101 Townsend St., San Francisco; Resend = Plus Five Five, Inc.). Nutzerin per Rückfrage entscheiden lassen: (1) Cookies-Inhalt wird **kein** eigenes `/cookies` mehr, sondern Unterabschnitt 5 der Datenschutzerklärung (Begründung: keine eigene Cookie-Policy gesetzlich nötig, Seite setzt aktuell keine Tracking-Cookies, Footer-Link "Cookies" zeigte im Code ohnehin schon auf `/datenschutz`) — daraufhin redundanten `{ label: "Cookies", href: "/datenschutz" }`-Eintrag aus `content/site.ts` `footer.links` entfernt; (2) Adresse "Varreler Bäke 37G" bestätigt; (3) DB-Region von Netlify DB/Neon ist noch unklar → Datenschutztext sicherheitshalber mit Drittstaaten-Klausel (SCCs) formuliert, als TODO markiert, sobald EU-Region bestätigt ist vereinfachbar; (4) E-Mail-Benachrichtigung bei Kontaktanfragen via Resend ist geplant (laut DEPLOYMENT.md aktuell nur vorbereitet, nicht implementiert) → entsprechender Absatz im Entwurf klar als "erst aktivieren, wenn Resend im Code läuft" markiert, nicht als aktueller Fakt behauptet. Ergebnis in neuer Datei `RECHTSTEXTE.md` im Repo-Root (kompletter Copy-paste-Text für Impressum + Datenschutzerklärung inkl. Cookies-Abschnitt, gedacht zum Einfügen in `/admin` → Globals → Impressum/Datenschutz → `body`-Rich-Text-Feld) plus Abschnitt "Offene Punkte" für die noch fehlenden Bestätigungen (Steuernummer/Kleinunternehmer optional, DB-Region, R2-Bucket-Jurisdiktion, Resend-Absatz scharfschalten). Reine Content-/Doku-Änderung, keine Layout-/Design-Umsetzung — die ImpressumPage-/DatenschutzPage-Payload-Globals (`body`-Feld) sind weiterhin leer, der Text muss noch manuell im Admin-Panel eingetragen werden (kein Seed-Skript-Pfad für diese beiden Globals vorhanden, anders als bei den übrigen `content/*.ts`-Dateien).
- Zuletzt erledigt (Session 7, /admin-500-Fix Root Cause): Live-`/admin` zeigte "This page couldn't load. Reload to try again, or go back." — sah wie ein Server-500 aus, war aber ein clientseitiger Hydration-Crash (kein `digest` an Next.js' `global-error.js`, curl auf `/admin`/`/admin/login` lieferte durchgehend valides 200-HTML). Ursache: `app/layout.tsx` (Root-Layout der Site, rendert eigenes `<html>`/`<body>`) wrappte `app/(payload)/layout.tsx`, dessen Payload-`RootLayout` (`@payloadcms/next/layouts`) ebenfalls ein eigenes `<html>`/`<body>` rendert — doppelt verschachteltes `<html>` auf jeder `/admin`-Route. Der Browser korrigiert das ungültige DOM beim Parsen, wodurch React beim Hydrieren clientseitig crasht. Fix nach Payloads dokumentiertem "Multiple Root Layouts"-Pattern: alle Frontend-Routen (`layout.tsx`, `page.tsx`, `not-found.tsx`, `globals.css`, alle Unterseiten) per `git mv` nach `app/(frontend)/` verschoben, sodass `(frontend)` und `(payload)` als gleichrangige, unabhängige Root-Layouts nebeneinander stehen (kein `app/layout.tsx` mehr auf true-root-Ebene). `icon.tsx`/`opengraph-image.tsx`/`robots.ts`/`sitemap.ts` bleiben auf true-root-Ebene (reine Metadata-Route-Handler, kein Root-Layout nötig). Reiner Rename, keine Inhaltsänderung (`git diff -M` zeigt 0 insertions/deletions). Lokal verifiziert via `next dev`: `/admin` + `/admin/login` liefern sauber 200 ohne Crash (vorher reproduzierte der Bug identisch auch im Dev-Server). Committed, noch nicht gepusht (User will Diff/Deploy selbst freigeben) — Live-Verifikation gegen `https://joanna-wildlight.netlify.app/admin` steht noch aus.
- Ebenfalls Session 7 (Priority 2, Navigation-UX-Politur): Mobile-Drawer in `components/layout/Header.tsx` hatte gar keinen Öffnen/Schließen-Übergang (`{open && createPortal(...)}` mountete/unmountete instant). Jetzt Fade + leichtes Absinken aus der Kopfzeile (`translateY(-16px)→0`, 220ms ease-in-out, `.jw-drawer`/`.jw-drawer-open` in globals.css) — bewusst an die bestehende PageTransition-Bewegungssprache angelehnt (Fade + Y-Verschiebung), nicht neu erfunden. Technisch: `mounted`+`entered`-State getrennt von `open`, damit (a) der geschlossene Ausgangszustand einen Frame lang gemalt wird, bevor auf „geöffnet" umgeschaltet wird (sonst spielt die CSS-Transition nicht ab) und (b) der Drawer erst nach Ablauf der Exit-Transition unmountet. Synchrone State-Anpassungen laufen zur Render-Zeit (React-Pattern, wie schon beim Pathname-Reset in derselben Datei) statt in einem Effekt — ein Effekt, der `setState` synchron im Body aufruft, hätte den `react-hooks/set-state-in-effect`-Lint-Fehler ausgelöst; der verbleibende Effekt macht nur noch echte externe Synchronisation (ein Frame abwarten via `requestAnimationFrame`, Unmount-Timer via `setTimeout`). Scroll-Lock läuft jetzt über die gesamte Mount-Dauer (inkl. Exit-Animation), nicht nur `open`, damit die Seite während des Ausblendens nicht springt. `prefers-reduced-motion` übersprint Verzögerung/Animation komplett (JS-seitig, nicht nur per CSS — sonst bliebe der Drawer bei reduced-motion-Nutzern unnötig 220ms sichtbar hängen, analog zum bestehenden PageTransition-Pattern). Verifiziert im Browser (390px, `next dev` gegen geseedete lokale DB): sichtbare Zwischenzustände beim Fade, sauberes Verhalten bei schnellem Mehrfach-Tap, Routenwechsel per Drawer-Link, Browser-Zurück während offenem Drawer (schließt automatisch über den bestehenden Pathname-Reset) — keine Konsolenfehler. `prefers-reduced-motion` per `matchMedia`-Override getestet: Drawer erscheint/verschwindet dann instant ohne Animation. PageTransition-180ms-Crossfade separat geprüft: reale Navigationszeit (RSC-Fetch+Commit) lokal ~93–116ms im Dev-Modus (ungecacht, worst case) — deutlich unter den 180ms, Produktion mit ISR (`revalidate=60`) sollte gleich oder schneller sein. Keine Evidenz, dass die Dauer nachjustiert werden muss; unverändert gelassen.
- Ebenfalls Session 7 (Priority 3a, Bild-Ladeuniformität): Bilder aus Payload/R2 hatten kein Blur-Placeholder (next/image `placeholder="blur"` fehlte komplett), deshalb poppte jedes Bild einzeln ein, sobald sein eigener Request durch war — das war das "nicht uniforme" Laden. Fix: `collections/Media.ts` bekommt ein `beforeChange`-Hook, das bei jedem Upload (Admin-UI und `payload.create({ filePath })`, z. B. `scripts/seed.ts` — beide Wege füllen `req.file.data` als Buffer vor dem Hook, geprüft in `node_modules/payload/dist/uploads/getFileByPath.js`) mit `sharp` ein 20px breites JPEG (Qualität 40) erzeugt und als Data-URI im neuen `blurDataURL`-Textfeld speichert (`admin.hidden`, rein programmatisch). Neue Migration `20260720_185846_media_blur_data_url` (nur `ALTER TABLE media ADD COLUMN blur_data_u_r_l varchar` — Payload pusht Schema in Prod bewusst nicht automatisch, siehe netlify.toml-Kommentar). `ResolvedImage` (lib/content/types.ts) um `blurDataURL: string | null` erweitert, `resolveImage()` in `lib/content/provider.ts` gibt es durch; `PlaceholderImage.tsx` reicht `placeholder="blur"`/`blurDataURL` an next/image weiter, wenn vorhanden (ältere/zukünftige Uploads ohne das Feld fallen sauber auf kein Blur zurück, kein Fehler). Lokal end-to-end verifiziert: DB-Reset (`docker compose down -v` + `up`) → `migrate` → `seed` → in Postgres geprüft, dass alle 13 hochgeladenen Bilder ein ~500–600 Zeichen langes `blur_data_u_r_l` haben; im gerenderten HTML tauchen 23 eingebettete Blur-JPEGs auf (Next rendert sie als geblurte Inline-SVG-Filter); im Browser-Screenshot beim Scrollen tatsächlich blurred Platzhalter vor dem vollen Bild erwischt. Nebenbei den in derselben Aufgabe verlangten Audit von `priority`/`sizes` gemacht: `sizes` war praktisch überall auf den PlaceholderImage-Default `100vw` (Next fordert dann immer die größte Bildbreite an, selbst in mehrspaltigen Grids) — korrigiert für die klarsten Fälle: „Kleine Werke"-Grid (`page.tsx`, 2/4-spaltig → `"(min-width:768px) 25vw, 50vw"`), „Was ich festhalte"-Karten (3-spaltig → `"(min-width:768px) 30vw, 100vw"`), Fotografie-Kategorie-Bilder (feste 520px-Spalte am Desktop → `CategoryCarousel` bekam dafür überhaupt erst eine `sizes`-Prop zum Durchreichen), Intro-Portraits auf Start-/Über-mich-Seite (feste 400–460px-Spalten → `ArchImage` bekam ebenfalls eine `sizes`-Prop). Per Netzwerk-Tab verifiziert: Werke-Grid fordert jetzt `w=384` statt vorher `w=1920` an (Next rundet auf die nächste konfigurierte Bildbreite). `priority` war schon vorher auf den offensichtlichen LCP-Kandidaten gesetzt (Hero-Slide 0, Start-/Über-mich-Intro-Portrait, erste Fotografie-Kategorie) — keine fehlenden Fälle gefunden, nicht angefasst.
- Ebenfalls Session 7 (Priority 3b, Desktop-Seitenverhältnis): Von den 8 in der Aufgabe genannten `aspect-auto md:h-[Npx]`-Stellen empirisch per `getBoundingClientRect()` bei 1280/1440/1920px nachgemessen statt blind zu pauschalisieren — Ergebnis überraschend gemischt: **5 von 8 waren bereits korrekt** und nicht Teil des Bugs (Home-Collage-Portrait, alle Fotografie-Kategorie-Bilder, Über-mich Intro-Portrait + Leidenschafts-Bild, Kontakt-Seitenportrait) — sie sitzen alle in Grid-Spalten mit einem **festen Px-Wert** (`md:grid-cols-[1fr_460px]`, `[520px_1fr]`, `[460px_1fr]`, `[1fr_400px]`, `[1fr_480px]`), wodurch die Bildbreite bei jeder Desktop-Viewport-Breite exakt gleich bleibt (gemessen: identische Werte bei 1280 und 1920, z. B. Fotografie 531×629px an beiden Breiten) — das per Aufgabe beschriebene "Seitenverhältnis wächst mit der Viewport-Breite" tritt dort schlicht nicht auf, angefasst wurde hier nichts. **3 von 8 hatten den echten Bug**, alle in Grids mit `fr`-basierten (fluiden) Spalten, wo die Breite tatsächlich mit dem Viewport wächst, während die Höhe fix blieb: HeroCarousel.tsx (`md:aspect-auto md:h-[680px]`, volle Breite fließend) → `md:aspect-[2/1]` (hält 2:1 konstant statt bei 1920px auf 2,8:1 aufzureißen — Foto zeigt jetzt bei breiten Screens mehr vom Motiv statt aggressiver zugeschnitten zu werden). „Was ich festhalte"-Karten (`grid-cols-3`, echte `1fr`-Spalten) und „Kleine Werke"-Grid (`grid-cols-4`) → beide auf durchgehendes `aspect-[3/4]` (wie mobil) umgestellt, dafür `md:max-w-[360px]`/`md:max-w-[300px]` + `md:mx-auto` auf den jeweiligen Karten-/Bild-Container (nicht auf die PlaceholderImage allein, sonst passt der Kartenhintergrund/Text nicht mehr zur Bildbreite) — Auto-Margins zentrieren die gecappte Karte in ihrer sonst breiteren Grid-Spalte (Standard-CSS-Grid-Verhalten: `justify-self:stretch` weicht Auto-Margins). Verifiziert bei 1280/1440/1920: Karten/Grid-Bilder halten jetzt konstant ihr Seitenverhältnis (vorher z. B. Festhalte-Karten 0,79 bei 1280 → 1,36 bei 1920, jetzt konstant ~0,75), Mobile-Ansicht (390px) unverändert/ungetestet-unauffällig (nur `md:`-Klassen angefasst).
- Neu gefunden, nicht Teil des Fixes (Session 7): (1) `npx next build` crasht lokal auf diesem macOS-Rechner reproduzierbar (jedes Mal, auf wechselnden Seiten `/qa` oder `/kontakt`) mit `TypeError: Cannot read properties of undefined (reading 'split')` während des parallelen Prerenderings (13 Worker) — reproduziert identisch auf unverändertem `main` vor diesem Fix, also nicht durch die Route-Group-Umstrukturierung verursacht. Verdächtig: Build-Log zeigt `objc[...]: Class GNotificationCenterDelegate is implemented in both .../node_modules/@img/sharp-libvips-darwin-arm64/...8.18.3... and .../node_modules/next/node_modules/@img/sharp-libvips-darwin-arm64/...8.17.3...` (zwei verschiedene sharp/libvips-Versionen im selben Prozess geladen, macOS warnt selbst vor "spurious casting failures and mysterious crashes") — passt zum Fehlerbild. Tritt auf Netlifys Linux-Build nicht auf (letzte 3 Deploys alle `state: ready`), daher kein Blocker für Priority 1, aber lokal nervig; falls es weiter stört: `node_modules/next/node_modules/@img/sharp-libvips-darwin-arm64` vs. Top-Level-Version angleichen/dedupen. (2) Lokale Postgres-DB (Docker, Port 5433) ist komplett leer (0 Zeilen in `home_page`/`media`/`users` — nie geseedet in dieser Session) → alle Content-Seiten (`/`, `/ueber-mich`, `/fotografie`, `/qa`, `/kontakt`) crashen lokal in `next dev` mit `TypeError` in `components/primitives/Accent.tsx:19` (`text.split()` auf undefined CMS-Feld), weil Payload keine Inhalte liefert. Kein Bug im Code, nur fehlender lokaler Seed (`npm run seed` gegen die lokale DB laufen lassen, falls lokale Content-Vorschau gebraucht wird).
- Zuletzt erledigt (Session 6, Payload-CMS-Migration + Mobile-Bugfixes): Komplette Migration von statischem GitHub-Pages-Export auf Next.js-SSR + eingebettetes Payload CMS (Postgres via Netlify DB/Neon, Medien via Cloudflare R2 S3-Adapter), gehostet auf Netlify. Alle Seiten-Inhalte als Payload-Globals modelliert (SiteSettings, HomePage, AboutPage, PhotographyPage, FaqPage, ContactPage, Impressum-/DatenschutzPage), Testimonials/FaqItems/PhotoCategories als eigene Collections mit `order`-Feld. `PayloadContentProvider` (lib/content/provider.ts) implementiert das bestehende `ContentProvider`-Interface über Payloads Local API — Komponenten/Seiten mussten nur beim Bildsystem angefasst werden (imageKey-Manifest-Lookup → direkt aufgelöste {url,alt}-Objekte aus Payload-Media-Relationships; `lib/images/manifest.ts` und `lib/basePath.ts` komplett entfernt). `scripts/seed.ts` überträgt die bisherige Copy (content/*.ts, jetzt nur noch Seed-Quelle) + alle 17 Fotos einmalig nach Payload — end-to-end gegen lokales Postgres verifiziert (Media/Testimonials/FaqItems/PhotoCategories-Counts geprüft, alle 5+2 Routen + /admin + /api/contact-submissions curl-getestet, `next build` inkl. ISR-Revalidate 1m auf allen Seiten). Kontaktformular postet jetzt an `/api/contact-submissions` (öffentlicher create, read nur eingeloggt — Access-Control verifiziert). Zwei explizit gemeldete Mobile-Bugs behoben: Menü-Drawer war Kind des sticky Headers mit `top-16`-Lücke (Hintergrund blitzte durch, wenn Sticky-Positionierung während schnellem Scrollen hinterherhinkte) → Drawer jetzt per Portal in den Body, deckt volle Viewport-Höhe inkl. eigener Kopfzeile ab, unabhängig vom Live-Zustand des echten Headers; Routenwechsel hatten keinerlei Übergang → `PageTransition`-Komponente (Crossfade, respektiert prefers-reduced-motion) in app/layout.tsx. Zusätzlich im Zuge der Recherche gefunden und behoben: fehlendes Impressum/Datenschutz (rechtlich in Deutschland relevant, Footer-Links liefen bisher auf href="#") → echte Routen + CMS-editierbare Globals (Rechtstext selbst fehlt noch, TODO für Joanna), fehlender Favicon/OG-Image/sitemap.xml/robots.txt/404-Seite ergänzt, Next-Image-Optimizer jetzt nutzbar (kein `unoptimized`/basePath mehr nötig) wodurch der manuelle srcMobile-`<picture>`-Hack in PlaceholderImage entfallen konnte.
- Blocker/Notizen Session 6: Payload-CLI (`payload run`/`generate:*`/`migrate:*`) scheiterte auf diesem Node-26-Setup mit `ERR_REQUIRE_ASYNC_MODULE` beim Laden von payload.config.ts (Top-Level-Await in @payloadcms/richtext-lexical, CJS-require via tsx) — behoben durch `"type": "module"` in package.json (Next selbst lief davor schon unabhängig davon fehlerfrei). Docker Desktop war zu Sessionbeginn nicht gestartet; `open -a Docker` + kurz warten reichte. Lokale Verifikation lief komplett gegen echtes Postgres (docker-compose.yml, Port 5433) statt SQLite, um Prod-Parität zu haben — Migration `migrations/20260720_160913_initial_schema.ts` wurde bereits generiert und muss beim ersten Netlify-Deploy nicht neu erzeugt werden (nur `payload migrate` läuft, siehe netlify.toml Build-Command).
- Offen (nicht von hier aus erledigbar): Netlify-Account-Setup, `netlify db init`, Cloudflare-R2-Bucket + Public-URL, PAYLOAD_SECRET/Env-Vars in Netlify, erster Admin-User im Live-/admin, echter Rechtstext für Impressum/Datenschutz, Produktions-Seed gegen die echte DB — alles Schritt für Schritt in DEPLOYMENT.md dokumentiert.
- Frühere Session (5, Mobile-First-Pass): Scroll-Snap nur noch auf Desktop mit Maus (`@media (min-width:768px) and (pointer:fine)`) — das `y proximity`-Snap auf `html` kämpfte in iOS Safari gegen das Momentum-Scrolling und machte das Scrollen ruckelig. iOS-sicherer Scroll-Lock in `lib/scrollLock.ts` (overflow auf Root-Element statt nur Body; kein position:fixed-Trick, der würde den Sticky-Header verschieben) — genutzt von Lightbox + Mobile-Drawer. Lightbox rendert jetzt per `createPortal` in den Body: rotierte Rahmen (Polaroid/Karten mit `transform`) bildeten den Containing Block für `position:fixed`, das Overlay blieb im Rahmen gefangen. Bildflächen mobil von festen px-Höhen auf `aspect-[3/4]` (Karten, Werke-Grid, Kategorie-Karussells, Arch-Portraits) bzw. `aspect-square` (Hero) umgestellt — deutlich weniger Beschnitt der Hochformat-Fotos. Hero-Bilder bekamen 1080px-Mobile-Varianten (~185KB statt ~470KB, LCP) via `srcMobile` im Manifest + `<picture>`-Zweig in PlaceholderImage (Lightbox nutzt weiter die volle Auflösung). Marquee-Wortlisten auf about/faq/photography aufgefüllt (waren nur 4 bzw. 6 Einträge). `-webkit-tap-highlight-color: transparent`.
- Frühere Session (4): Intro-Collage-Bug behoben (ArchImage bekam `absolute` in die intern `relative` Fläche — `.relative` gewinnt in der CSS-Reihenfolge, Bogen-Portrait kollabierte auf 0px; jetzt Wrapper-Div positioniert). Marquee: mehr Wörter + selbstskalierende Wiederholung/Dauer (Leerraum auf breiten Viewports behoben). Fotografie: CategoryCarousel je Sektion (Crossfade, klickbare Dots) + Lightbox (Klick vergrößert, Esc/Klick schließt); Manifest um kat-*-2/3-Slides ergänzt, PhotoCategory.imageKey → imageKeys[]. Hero-/Testimonial-Dots jetzt klickbare Buttons. Scroll: smooth + scroll-snap y proximity auf Sektionsanfänge (scroll-padding = Headerhöhe). Logo-PNG randlos beschnitten (war 283×129 mit Wortmark nur 147×57) und 2x gespeichert → wirkt im Header design-groß ohne Header-Vergrößerung. Danach: Lightbox-Zoom auf ALLE Content-Bilder ausgeweitet — PlaceholderImage ist jetzt Client-Komponente mit `expandable` (Default an, gilt via ArchImage/Polaroid überall); Lightbox nach components/primitives/ verschoben; Hero-Titel + inaktive Slides pointer-events-none, damit der Zoom-Klick durchkommt.
- In Arbeit / Als Nächstes: offen bleiben Inhalts-TODOs: 2 echte Testimonials, Mail-Service in lib/contact/submit.ts, Datenschutz/Impressum-Links im Footer (href="#"); Logo-Quelle ist nur 155×65 — für gestochen scharfes Retina wäre ein höher aufgelöster Export gut.
- Deployment: Pages lief kurz auf build_type "legacy" (Branch-Root via Jekyll → README wurde als Startseite gerendert). Per `gh api -X PUT repos/VossFynn/PortfolioJoannaWildlight/pages -f build_type=workflow` auf GitHub Actions umgestellt (2026-07-19); Live-Site verifiziert. Falls je wieder die README erscheint: Settings → Pages → Source muss "GitHub Actions" sein.
- Blocker/Notizen: Tailwind v4 → Theme-Mapping in globals.css via @theme inline (kein tailwind.config). Achtung: `rtk next build` meldete einmal fälschlich Erfolg aus stalem Zustand — zur Verifikation `npx next build` direkt nutzen und Routen-Liste prüfen. Die vom User gelieferten Bilddateien waren AVIF mit falscher Endung → per sips zu echten JPEG/PNG konvertiert. WICHTIG: PlaceholderImage/ArchImage nie `absolute` in className geben (Position-Konflikt) — Wrapper verwenden.
- Aktualisiert: Session 12, 2026-07-21

---

### Phase 0 — Setup
- [x] Next.js (App Router) + TS strict + Tailwind initialisiert; `npm run dev` läuft fehlerfrei
- [x] Fonts (Cormorant Garamond, Mulish) via next/font eingebunden
- [x] Ordnerstruktur: app/, components/{primitives,layout,sections}, content/, lib/content/, lib/images
- [x] PROGRESS.md aus dieser Checkliste erstellt + STAND-BLOCK gesetzt

### Phase 1 — Design-Tokens
- [x] Alle Tokens aus README (Farben, Radien, Schatten, Button-Höhen, Trennstrich, Typo-Skalen Desktop/Mobile) als CSS-Custom-Properties in globals.css
- [x] Tokens in Tailwind-Theme gemappt; Referenzierung nur über Tokens

### Phase 2 — Content-Schicht (CMS-ready)
- [x] TS-Typen: Testimonial, FaqItem, PhotoCategory, NavItem, PageMeta, HeroImage, ContactSource
- [x] content/-Daten mit echter Copy aus den .dc.html (site, home, about, photography, faq, testimonials)
- [x] ContentProvider-Interface + local-provider; async Getter: getHeroImages, getTestimonials, getFaqItems, getPhotoCategories, getPageContent

### Phase 3 — Bilder
- [x] Bild-Manifest: semantischer Key → { src: null, label, motiv/format, ratio }
- [x] Placeholder-Komponente (ohne src: gestreift + Monospace-Label + warmer Radial-Glow; mit src: next/image ohne Glow)

### Phase 4 — Primitive
- [x] Button (Varianten: Outline dunkel, Gefüllt dunkel, Outline gold auf dunklem Grund) + Hover-Transitions
- [x] Marquee/Laufband (endlos ~30s, Inhalt dupliziert, pausiert bei prefers-reduced-motion)
- [x] StickerBadge, Polaroid, ArchImage (Bogen-Radius), SunCircle, SectionDivider (48×1px Gold)
- [x] ScrollReveal (IntersectionObserver, einmalig, opt. Stagger; bei reduced-motion deaktiviert)

### Phase 5 — Layout
- [x] Header/Nav (aktive Seite Gold-Unterstrich, Hover gold; Mobile-Burger → Fullscreen-Drawer im gleichen Stil)
- [x] Footer
- [x] CTABand-Komponente (wiederverwendbar, Gold-Glow, Gold-Outline-Button)

### Phase 6 — Hero-Carousel
- [x] HeroCarousel: Crossfade, Auto-Advance 5–6s endlos, Overlay-Verlauf + zentrierter Text bleiben fix über allen Slides, prefers-reduced-motion = statisch erstes Bild, optional dezente Dots, a11y (aria-live polite, Alt/Label)

### Phase 7 — Seiten (Copy 1:1 aus .dc.html, Akzentwörter als italic-Gold-<span>)
- [x] Startseite (Hero-Carousel, Intro+Collage, Laufband, "festhalte"-Karten, "Werke"-Grid mit Versatz, Testimonials, Trust-Zeile, CTA-Band, Footer)
- [x] Über mich
- [x] Fotografie (6 alternierende Kategorie-Sektionen, exakte Copy + Rotationen + Wechsel-BG)
- [x] Q+A (Desktop: 9 Karten offen; Mobile: Accordion, eine offen, erste initial offen)
- [x] Kontakt (Formular + Client-Validierung, Quellen-Chips Single-Select, Datenschutz-Checkbox Pflicht, Submit gegen austauschbaren Server-Action/Route-Stub mit TODO für Mail-Service)

### Phase 8 — Interaktionen & Feinschliff
- [x] Testimonial-Karussell (Pfeile + Dots, 3 Einträge, opt. Auto-Rotate 6s)
- [x] Responsive gegen 1440/390 geprüft (Grids einspaltig, Bogen-Portraits mobil max ~300px zentriert)
- [x] a11y: semantisches HTML, Alt aus Labels, Tastaturbedienung, sichtbare Focus-States
- [x] prefers-reduced-motion überall respektiert

### Phase 9 — Doku & Abschluss
- [x] PROJECT_README.md: Architektur/Ordnerstruktur + Run-Befehle; Platzhalter→echte Fotos ersetzen (Manifest, Keys, Schritte); wo Copy liegt + wie bearbeiten; CMS-Naht (welches Interface implementieren, was ein Adapter erfüllen muss); wo Tokens liegen
- [x] `npm run build` + Lint + Typecheck sauber
- [x] "Getroffene Entscheidungen"-Notiz in PROGRESS.md ergänzt

---

## Getroffene Entscheidungen
- Projekt-Root = `/Users/U730282/.private/PortfolioJoannaWildlight/` (Next.js-App direkt im Root, Design-Handoff bleibt in `design_handoff_joanna_wildlight/`).
- Testimonials: Das Design enthält nur 1 echtes Zitat (Sarah und Sven), zeigt aber 3 Dots. Einträge 2+3 sind weiterhin Platzhalter-Text — jetzt aber direkt in `/admin` unter Testimonials editierbar, kein Code-Deploy mehr nötig.
- ContentProvider: `getContentProvider()` in `lib/content/provider.ts` ist die einzige Austauschstelle für den Content-Adapter; seit Session 6 implementiert `PayloadContentProvider` das Interface über Payloads Local API (vorher `LocalContentProvider` mit dynamischem Import aus `content/`).
- Kontakt-Submit (korrigiert, Session 6): `submitContactRequest` liegt in `lib/contact/submit.ts` (Client-seitiger Fetch, keine Server Action) und postet an `/api/contact-submissions` (Payload-REST, öffentlicher create-Zugriff).
- Payload/Netlify-Stack (Session 6, mit Nutzer abgestimmt): Postgres via Netlify DB (Neon, ohne separaten Account) statt MongoDB/Turso; Medien via Cloudflare R2 (S3-Adapter, keine Egress-Kosten) statt Uploadthing/S3. Begründung: Netlify DB lässt sich direkt aus dem Netlify-Dashboard provisionieren (ein Account weniger), R2 passt gut zu einem foto-lastigen Portfolio (viel Traffic auf große Bilder, R2 verlangt dafür nichts extra).
- Platzhalter-Streifen in 3 Tönen (ivory/greige/card) als Tokens, exakt nach den drei Streifen-Paaren im Design; Karten-/Polaroid-Flächen nutzen `tone="card"`.
- Hero-Carousel: 5,5s-Intervall, 1s-Crossfade; Dots werden bei prefers-reduced-motion per `motion-reduce:hidden` ausgeblendet (statisch = erstes Bild).
- Q+A mobil: Accordion mit genau einer offenen Frage (erste initial), erneutes Tippen schließt (`open = -1`), wie im data-dc-script der Referenz.
- Kontakt-Submit: Server Action `submitContactRequest` in `app/kontakt/actions.ts` als Stub (loggt + validiert minimal); TODO Mail-Service dort dokumentiert, Signatur stabil halten.
- Fehlermeldungen im Formular: italic Gold-Dark (Design definiert keine Fehlerfarbe).
- Footer-Sonderfarben (#EDEAE0/#A8A190/#C0B9A6/#7E786A) als eigene Tokens ergänzt statt Hex in der Komponente.
